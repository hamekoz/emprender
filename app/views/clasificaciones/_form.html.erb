<%= form_for(@clasificacion) do |f| %>
  <% if @clasificacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clasificacion.errors.count, "error") %> <%= t('errores.guardar.f')%> clasificacion:</h2>

      <ul>
      <% @clasificacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
