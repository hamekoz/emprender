<h1>Listando de personas</h1>

<table>
  <tr>
    <th>Nombres</th>
    <th>Apellidos</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @personas.each do |persona| %>
  <tr>
    <td><%= persona.nombre %></td>
    <td><%= persona.apellido %></td>
    <td><%= link_to 'Mostrar', persona %></td>
    <td><%= link_to 'Editar', edit_persona_path(persona) %></td>
    <td><%= link_to 'Eliminar', persona, :confirm => '¿Esta seguro?', :method => :delete %></td>
    <br />
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Persona', new_persona_path %>
