<%= form_for(@institucion) do |f| %>
  <% if @institucion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institucion.errors.count, "error") %> prohibited this institucion from being saved:</h2>

      <ul>
      <% @institucion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag "Nombre" %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= label_tag "Descripcion" %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= submit_tag 'Guardar' %>
  </div>
<% end %>
