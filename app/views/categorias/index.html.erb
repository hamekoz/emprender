<h1>Listando categorias</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Descripcion</th>
    <th colspan="3" rowspan="1">
      <%= boton_agregar new_categoria_path %>
    </th>
  </tr>

<% @categorias.each do |categoria| %>
  <tr>
    <td><%= categoria.nombre %></td>
    <td><%= categoria.descripcion %></td>
    <td><%= boton_mostrar categoria %></td>
    <td><%= boton_editar edit_categoria_path(categoria) %></td>
    <td><%= boton_eliminar categoria %></td>
  </tr>
<% end %>
</table>

<br />
