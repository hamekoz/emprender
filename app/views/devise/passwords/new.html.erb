<%
  add_crumb "Inicio", root_path
  add_crumb "Olvide mi contraseña"
%>

<h1 class="page-header">¿Olvido su contraseña?</h1>
<div class="row">
  <div class="span8 offset2">

  <p class="alert alert-info">Si no recuerda su contraseña podemos enviarle a su correo las instrucciones para poder recuperarla</p>
  <%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post,:class => 'form-horizontal' }) do |f| %>
    <%= f.error_notification %>

    <div class="inputs well">
      <%= f.input :email, :required => true %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Enviarme las instrucciones", :class => 'btn btn-primary' %>
    </div>
  <% end %>

  <%= render :partial => "devise/shared/links" %>
  </div>
</div>
