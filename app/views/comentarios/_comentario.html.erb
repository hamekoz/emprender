<%= div_for comentario, :class => (comentario.aceptado ? "alert alert-success" : "alert") do %>
  <%= l(comentario.created_at, :format => :simple) %>: <strong><%= comentario.texto %></strong>
<% end %>
