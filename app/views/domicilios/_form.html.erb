<%= form_for(@domicilio) do |f| %>
  <% if @domicilio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@domicilio.errors.count, "error") %> prohibited this domicilio from being saved:</h2>

      <ul>
      <% @domicilio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
