<%= simple_form_for @estado do |f| %>

  <%= f.error_notification %>

  <fieldset>
    <legend>Estado:</legend>
    <%= f.input :nombre %>
    <%= f.input :descripcion %>
  </fieldset>

  <div class="actions">
    <%= f.button :submit, :class => 'btn primary' %>
    <%= boton_cancelar estados_path %>
  </div>

<% end %>
