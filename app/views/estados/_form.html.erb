<%= form_for(@estado) do |f| %>
  <% if @estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado.errors.count, "error") %> <%= t('errores.guardar.m')%> estado:</h2>

      <ul>
      <% @estado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<fieldset>
  <div class="clearfix">
    <%= f.label :nombre %>
    <div class="input">
      <%= f.text_field :nombre %>
    </div>
  </div>
  <div class="clearfix">
    <%= f.label :descripcion %>
    <div class="input">
      <%= f.text_field :descripcion %>
    </div>
  </div>
</fieldset>
  <div class="actions">
    <%= f.submit :class => 'btn primary' %>
    <%= boton_cancelar :back %>
  </div>
<% end %>
